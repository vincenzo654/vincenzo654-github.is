const menu=document.querySelector(".menu"),nav=document.querySelector("nav");menu?.addEventListener("click",()=>nav.classList.toggle("open"));document.querySelectorAll("nav a").forEach(a=>a.addEventListener("click",()=>nav.classList.remove("open")));const toast=document.getElementById("toast");document.querySelectorAll(".select,.plans button").forEach(b=>b.addEventListener("click",()=>{toast.textContent=(b.dataset.name||b.textContent)+" selected ✓";toast.classList.add("show");setTimeout(()=>toast.classList.remove("show"),1800)}));document.getElementById("year").textContent=new Date().getFullYear();